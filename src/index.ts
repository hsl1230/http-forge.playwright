/**
 * @http-forge/playwright
 * 
 * Runtime package for HTTP Forge generated Playwright API clients.
 * Provides lightweight environment management and shared types.
 */

// Export ForgeEnv and interface
export { ForgeEnv, type IForgeEnv } from './forge-env';

// Re-export shared types
export type { BaseApiOptions, BaseRequestContext, HttpHeaders } from './shared-types';

